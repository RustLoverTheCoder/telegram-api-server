use anyhow::Error;

// account.getAutoSaveSettings
pub async fn get_auto_save_settings() -> Result<(), Error> {
    Ok(())
}

// account.saveAutoSaveSettings
pub async fn save_auto_save_settings() -> Result<(), Error> {
    Ok(())
}

// account.deleteAutoSaveExceptions
pub async fn delete_auto_save_exceptions() -> Result<(), Error> {
    Ok(())
}
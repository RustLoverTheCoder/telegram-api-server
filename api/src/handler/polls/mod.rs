use anyhow::Error;

// messages.getPollResults
pub async fn get_poll_results() -> Result<(), Error> {
    Ok(())
}

// messages.getPollVotes
pub async fn get_poll_votes() -> Result<(), Error> {
    Ok(())
}


// messages.sendVote
pub async fn send_vote() -> Result<(), Error> {
    Ok(())
}
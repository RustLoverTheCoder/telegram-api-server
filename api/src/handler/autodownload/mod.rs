use anyhow::Error;

// account.getAutoDownloadSettings
pub async fn get_auto_download_settings() -> Result<(), Error> {
    Ok(())
}

// account.saveAutoDownloadSettings
pub async fn save_auto_download_settings() -> Result<(), Error> {
    Ok(())
}
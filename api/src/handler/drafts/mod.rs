use anyhow::Error;

// messages.clearAllDrafts
pub async fn clear_all_drafts() -> Result<(), Error> {
    Ok(())
}

// messages.getAllDrafts
pub async fn get_all_drafts() -> Result<(), Error> {
    Ok(())
}

// messages.saveDraft
pub async fn save_draft() -> Result<(), Error> {
    Ok(())
}
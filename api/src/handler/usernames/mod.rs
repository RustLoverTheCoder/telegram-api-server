use anyhow::Error;

// account.reorderUsernames
pub async fn account_reorder_usernames() -> Result<i32, Error> {
    Ok(1)
}

// account.toggleUsername
pub async fn account_toggle_username() -> Result<i32, Error> {
    Ok(1)
}

// bots.reorderUsernames
pub async fn bots_reorder_usernames() -> Result<i32, Error> {
    Ok(1)
}

// bots.toggleUsername
pub async fn bots_toggle_username() -> Result<i32, Error> {
    Ok(1)
}

// channels.reorderUsernames
pub async fn channels_reorder_usernames() -> Result<i32, Error> {
    Ok(1)
}

// channels.toggleUsername
pub async fn channels_toggle_username() -> Result<i32, Error> {
    Ok(1)
}

// channels.deactivateAllUsernames
pub async fn channels_deactivate_all_usernames() -> Result<i32, Error> {
    Ok(1)
}
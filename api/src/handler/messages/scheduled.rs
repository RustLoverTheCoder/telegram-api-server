use anyhow::Error;

// messages.sendScheduledMessages
pub async fn send_scheduled_messages() -> Result<(), Error> {
    Ok(())
}

// messages.getScheduledHistory
pub async fn get_scheduled_history() -> Result<(), Error> {
    Ok(())
}

// messages.deleteScheduledMessages
pub async fn delete_scheduled_messages() -> Result<(), Error> {
    Ok(())
}

// messages.getScheduledMessages
pub async fn get_scheduled_messages() -> Result<(), Error> {
    Ok(())
}
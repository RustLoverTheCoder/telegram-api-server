use anyhow::Error;

// messages.translateText
pub async fn messages_translate_text() -> Result<(), Error> {
    Ok(())
}

// messages.togglePeerTranslations
pub async fn messages_toggle_peer_translations() -> Result<(), Error> {
    Ok(())
}


// messages.transcribeAudio
pub async fn messages_transcribe_audio() -> Result<(), Error> {
    Ok(())
}

// messages.rateTranscribedAudio
pub async fn messages_rate_transcribed_audio() -> Result<(), Error> {
    Ok(())
}
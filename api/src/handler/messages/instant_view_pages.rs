use anyhow::Error;

// messages.getWebPage
pub async fn messages_get_web_page() -> Result<(), Error> {
    Ok(())
}

// messages.getWebPagePreview
pub async fn messages_get_web_page_preview() -> Result<(), Error> {
    Ok(())
}

use anyhow::Error;

// messages.getSavedGifs
pub async fn messages_get_saved_gifs() -> Result<i32, Error> {
    Ok(1)
}

// messages.saveGif
pub async fn messages_save_gif() -> Result<i32, Error> {
    Ok(1)
}
use anyhow::Error;

// messages.getCustomEmojiDocuments
pub async fn messages_get_custom_emoji_documents() -> Result<(), Error> {
    Ok(())
}

// messages.getEmojiStickers
pub async fn messages_get_emoji_stickers() -> Result<(), Error> {
    Ok(())
}

// messages.getFeaturedEmojiStickers
pub async fn messages_get_featured_emoji_stickers() -> Result<(), Error> {
    Ok(())
}

// messages.getEmojiGroups
pub async fn messages_get_emoji_groups() -> Result<(), Error> {
    Ok(())
}

// messages.getEmojiStatusGroups
pub async fn messages_get_emoji_status_groups() -> Result<(), Error> {
    Ok(())
}

// messages.getEmojiProfilePhotoGroups
pub async fn messages_get_emoji_profile_photo_groups() -> Result<(), Error> {
    Ok(())
}

// messages.searchCustomEmoji
pub async fn messages_search_custom_emoji() -> Result<(), Error> {
    Ok(())
}

// account.getDefaultProfilePhotoEmojis
pub async fn account_get_default_profile_photo_emojis() -> Result<(), Error> {
    Ok(())
}

// account.getDefaultGroupPhotoEmojis
pub async fn account_get_default_group_photo_emojis() -> Result<(), Error> {
    Ok(())
}
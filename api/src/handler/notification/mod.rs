pub mod sounds;
use anyhow::Error;

// account.registerDevice
pub async fn register_device() -> Result<(), Error> {
    Ok(())
}

// account.unregisterDevice
pub async fn unregister_device() -> Result<(), Error> {
    Ok(())
}

// account.updateDeviceLocked
pub async fn update_device_locked() -> Result<(), Error> {
    Ok(())
}

// account.getNotifyExceptions
pub async fn get_notify_exceptions() -> Result<(), Error> {
    Ok(())
}

// account.getNotifySettings
pub async fn get_notify_settings() -> Result<(), Error> {
    Ok(())
}

// account.updateNotifySettings
pub async fn update_notify_settings() -> Result<(), Error> {
    Ok(())
}

// account.resetNotifySettings
pub async fn reset_notify_settings() -> Result<(), Error> {
    Ok(())
}

use anyhow::Error;

// channels.getSponsoredMessages
pub async fn get_sponsored_messages() -> Result<(), Error> {
    Ok(())
}

// channels.viewSponsoredMessage
pub async fn view_sponsored_message() -> Result<(), Error> {
    Ok(())
}
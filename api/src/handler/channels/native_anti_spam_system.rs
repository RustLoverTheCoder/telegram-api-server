use anyhow::Error;

// channels.toggleAntiSpam
pub async fn toggle_anti_spam() -> Result<(), Error> {
    Ok(())
}

// channels.reportAntiSpamFalsePositive
pub async fn report_anti_spam_false_positive() -> Result<(), Error> {
    Ok(())
}
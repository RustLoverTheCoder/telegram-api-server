use anyhow::Error;

// 2FA
// account.confirmPasswordEmail
pub async fn account_confirm_password_email() -> Result<(), Error> {
    Ok(())
}

// account.resendPasswordEmail
pub async fn account_resend_password_email() -> Result<(), Error> {
    Ok(())
}

// account.cancelPasswordEmail
pub async fn account_cancel_password_email() -> Result<(), Error> {
    Ok(())
}

// account.getPassword
pub async fn account_get_password() -> Result<(), Error> {
    Ok(())
}

// account.getPasswordSettings
pub async fn account_get_password_settings() -> Result<(), Error> {
    Ok(())
}

// account.updatePasswordSettings
pub async fn account_update_password_settings() -> Result<(), Error> {
    Ok(())
}

// account.declinePasswordReset
pub async fn account_decline_password_reset() -> Result<(), Error> {
    Ok(())
}
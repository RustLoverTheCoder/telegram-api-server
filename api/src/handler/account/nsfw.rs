use anyhow::Error;

// account.getContentSettings
pub async fn get_content_settings() -> Result<(), Error> {
    Ok(())
}

// account.setContentSettings
pub async fn set_content_settings() -> Result<(), Error> {
    Ok(())
}
use anyhow::Error;

// photos.deletePhotos
pub async fn delete_photos() -> Result<(), Error> {
    Ok(())
}

// photos.getUserPhotos
pub async fn get_user_photos() -> Result<(), Error> {
    Ok(())
}

// photos.updateProfilePhoto
pub async fn update_profile_photo() -> Result<(), Error> {
    Ok(())
}

// photos.uploadProfilePhoto
pub async fn upload_profile_photo() -> Result<(), Error> {
    Ok(())
}

// photos.uploadContactProfilePhoto
pub async fn upload_contact_profile_photo() -> Result<(), Error> {
    Ok(())
}